<?xml version="1.0" encoding="utf-8"?>
<!-- Autogenerated with mklanguages. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="ca"/> <!-- Catalan -->
    <locale android:name="tzm-Tfng"/> <!-- Central Atlas Tamazight (Tifinagh) -->
    <locale android:name="cs"/> <!-- Czech -->
    <locale android:name="cy"/> <!-- Welsh -->
    <locale android:name="de"/> <!-- German -->
    <locale android:name="en"/> <!-- English -->
    <locale android:name="en-GB"/> <!-- English (United Kingdom) -->
    <locale android:name="es"/> <!-- Spanish -->
    <locale android:name="eo"/> <!-- Esperanto -->
    <locale android:name="eu"/> <!-- Basque -->
    <locale android:name="fr"/> <!-- French -->
    <locale android:name="fy"/> <!-- Western Frisian -->
    <locale android:name="ga"/> <!-- Irish -->
    <locale android:name="gd"/> <!-- Scottish Gaelic -->
    <locale android:name="gl"/> <!-- Galician -->
    <locale android:name="in"/> <!-- Indonesian -->
    <locale android:name="is"/> <!-- Icelandic -->
    <locale android:name="it"/> <!-- Italian -->
    <locale android:name="lv"/> <!-- Latvian -->
    <locale android:name="lt"/> <!-- Lithuanian -->
    <locale android:name="lmo"/> <!-- Lombard -->
    <locale android:name="hu"/> <!-- Hungarian -->
    <locale android:name="nl"/> <!-- Dutch -->
    <locale android:name="nb-NO"/> <!-- Norwegian Bokmål (Norway) -->
    <locale android:name="nn"/> <!-- Norwegian Nynorsk -->
    <locale android:name="oc"/> <!-- Occitan -->
    <locale android:name="pl"/> <!-- Polish -->
    <locale android:name="pt-BR"/> <!-- Portuguese (Brazil) -->
    <locale android:name="pt-PT"/> <!-- Portuguese (Portugal) -->
    <locale android:name="sk"/> <!-- Slovak -->
    <locale android:name="sl"/> <!-- Slovenian -->
    <locale android:name="fi"/> <!-- Finnish -->
    <locale android:name="sv"/> <!-- Swedish -->
    <locale android:name="kab"/> <!-- Kabyle -->
    <locale android:name="vi"/> <!-- Vietnamese -->
    <locale android:name="tr"/> <!-- Turkish -->
    <locale android:name="el"/> <!-- Greek -->
    <locale android:name="be"/> <!-- Belarusian -->
    <locale android:name="bg"/> <!-- Bulgarian -->
    <locale android:name="ru"/> <!-- Russian -->
    <locale android:name="uk"/> <!-- Ukrainian -->
    <locale android:name="iw"/> <!-- Hebrew -->
    <locale android:name="ar"/> <!-- Arabic -->
    <locale android:name="fa"/> <!-- Persian -->
    <locale android:name="ckb"/> <!-- Central Kurdish -->
    <locale android:name="sa"/> <!-- Sanskrit -->
    <locale android:name="hi"/> <!-- Hindi -->
    <locale android:name="bn-BD"/> <!-- Bangla (Bangladesh) -->
    <locale android:name="bn-IN"/> <!-- Bangla (India) -->
    <locale android:name="or"/> <!-- Odia -->
    <locale android:name="ta"/> <!-- Tamil -->
    <locale android:name="ml"/> <!-- Malayalam -->
    <locale android:name="si"/> <!-- Sinhala -->
    <locale android:name="th"/> <!-- Thai -->
    <locale android:name="my"/> <!-- Burmese -->
    <locale android:name="ko"/> <!-- Korean -->
    <locale android:name="zh-CN"/> <!-- Chinese (China) -->
    <locale android:name="zh-MO"/> <!-- Chinese (Macao SAR China) -->
    <locale android:name="zh-HK"/> <!-- Chinese (Hong Kong SAR China) -->
    <locale android:name="zh-TW"/> <!-- Chinese (Taiwan) -->
    <locale android:name="zh-SG"/> <!-- Chinese (Singapore) -->
    <locale android:name="ja"/> <!-- Japanese -->
</locale-config>
