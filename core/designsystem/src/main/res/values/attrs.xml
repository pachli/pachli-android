<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="ClickableSpanTextView">
        <!-- Necessary to support tools:text in Android Studio layout designer -->
        <attr name="android:text"/>
    </declare-styleable>

    <declare-styleable name="SliderPreference">
        <attr name="android:value" format="string|reference" />
        <attr name="android:valueFrom" format="string|reference" />
        <attr name="android:valueTo" format="string|reference" />
        <attr name="android:stepSize" format="string|reference" />
        <attr name="format" format="string|reference" />
        <attr name="iconStart" format="reference" />
        <attr name="iconEnd" format="reference" />
    </declare-styleable>

    <!--Themed Attributes-->
    <attr name="textColorDisabled" format="reference|color" />
    <attr name="iconColor" format="reference|color" />
    <attr name="windowBackgroundColor" format="reference|color" />

    <attr name="status_text_small" format="dimension" />
    <attr name="status_text_medium" format="dimension" />
    <attr name="status_text_large" format="dimension" />

    <!--
      Height of a toolbar that contains a title and a subtitle.
      Some custom fonts require this to be larger than the default -->
    <attr name="actionBarSizeWithSubtitle" format="dimension|reference" />

    <!-- Colour to use for the "favourite" icon. Also displayed next to
         favourite notifications. -->
    <attr name="favoriteIconColor" format="color" />

    <declare-styleable name="PronounsChip">
        <attr name="pronounsChipStyle" format="reference" />
    </declare-styleable>

    <attr name="pollViewStyle" format="reference" />
    <declare-styleable name="PollView">
        <attr name="android:textSize" format="reference|dimension" />
    </declare-styleable>

    <attr name="previewCardViewStyle" format="reference" />
    <declare-styleable name="PreviewCardView">
        <attr name="android:background" />
        <attr name="android:foreground" />

        <!-- Background of the "info" part of the preview (contains the text
             content. -->
        <attr name="previewCardInfoBackground" format="reference|color" />
        <!-- Height of the preview image, if the image is stacked vertically above
             the preview content. -->
        <attr name="previewCardImageVerticalHeight" format="reference|dimension" />
        <!-- Width of the preview image, if the image is laid out horizontally next
             to the preview content. -->
        <attr name="previewCardImageHorizontalWidth" format="reference|dimension" />

        <attr name="previewCardTitleTextColor" format="reference|color" />
        <attr name="previewCardTitleTextSize" format="reference|dimension" />

        <attr name="previewCardDescriptionTextColor" format="reference|color" />
        <attr name="previewCardDescriptionTextSize" format="reference|dimension" />

        <attr name="previewCardAuthorTextColor" format="reference|color" />
        <attr name="previewCardAuthorTextSize" format="reference|dimension" />

        <attr name="previewCardAuthorTimelineLinkTextColor" format="reference|color" />
        <attr name="previewCardAuthorTimelineLinkTextSize" format="reference|dimension" />

        <attr name="previewCardAvatarSize" format="reference|dimension" />
        <attr name="previewCardAvatarCornerRadius" format="reference|dimension" />
    </declare-styleable>

    <attr name="collectionCardViewStyle" format="reference" />
    <declare-styleable name="CollectionCardView">
        <attr name="android:background" />
        <attr name="android:foreground" />

        <!-- Background of the "info" part of the collection (contains the text
             content. -->
        <attr name="collectionCardBackground" format="reference|color" />

        <attr name="collectionCardNameTextColor" format="reference|color" />
        <attr name="collectionCardNameTextSize" format="reference|dimension" />

        <attr name="collectionCardOwnerTextColor" format="reference|color" />
        <attr name="collectionCardOwnerTextSize" format="reference|dimension" />

        <attr name="collectionCardDescriptionTextColor" format="reference|color" />
        <attr name="collectionCardDescriptionTextSize" format="reference|dimension" />

        <attr name="collectionCardItemCountTextColor" format="reference|color" />
        <attr name="collectionCardItemCountTextSize" format="reference|dimension" />

        <attr name="collectionCardAvatarSize" format="reference|dimension" />
        <attr name="collectionCardAvatarCornerRadius" format="reference|dimension" />
    </declare-styleable>
</resources>
